require 'spec_helper'

describe Twingly::Amqp do
  it 'has a version number' do
    expect(Twingly::Amqp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
